<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onlinetea.prower.mapper.UserInfoMapper">

    <select id="queryUserInfoById" resultType="com.onlinetea.prower.Bean.UserInfo">
        SELECT c_id as cId, user_name as userName,user_password as userPassword,user_phone as userPhone,
        c_type as cType,user_sex as userSex,create_time as createTime
        FROM user
        WHERE c_id = #{userId}
    </select>

    <select id="queryUserInfoByName" resultType="com.onlinetea.prower.Bean.UserInfo">
        SELECT c_id as cId, user_name as userName,user_password as userPassword,user_phone as userPhone,
        c_type as cType,user_sex as userSex,create_time as createTime
        FROM user
        WHERE user_name = #{userName}
    </select>

    <insert id="logonUser" parameterType="com.onlinetea.prower.Bean.UserInfo">
        INSERT INTO user
        (user_name,user_password,user_phone,create_time,c_type,user_sex,salt_uuid)
        VALUES
        (#{userName},#{userPassword},#{userPhone},#{createTime},#{cType},#{userSex},#{saltUuid})
    </insert>

</mapper>